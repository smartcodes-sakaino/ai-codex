# プロジェクト共通ルール

このファイルはプロジェクトルートに配置してください。Claude Codeがセッション開始時に自動で読み込む共通ルールです。
`dev-kickoff` / `build-kickoff` / `test-runner` / `migration` の4つのSkills共通で適用されます。

## コマンド実行時のルール

ディレクトリ作成、ファイル操作、`git`や`npm`などのコマンド実行など、承認ダイアログ(「実行を許可しますか？」的な確認)が表示される操作を行う前には、**必ずその直前に「何をするための操作か」を平易な日本語で一文説明してから実行すること。**

- 専門用語(ディレクトリ、パス、コミット等)はできるだけ避け、初心者にも伝わる言葉を使う
- 例:「設計書というディレクトリを作成するための操作です」「作成したファイルの変更履歴を記録するための操作です」
- コマンドの内容をそのまま読み上げるのではなく、目的・結果を伝えることを優先する

## 秘密情報の取り扱いルール

APIキー、サービスアカウントの鍵、パスワードなどの秘密情報は、**チャット上に直接貼り付けない。必ずファイル(`.env`等)上でのみやり取りする。**

- 秘密情報が必要な場面では、「〇〇というファイルに値を貼り付けてください」のように案内し、チャット欄への入力は求めない
- 鍵のような長い文字列をコピー&ペーストしてもらう際は、「前後の引用符(`"`や`'`)は含めずに、値だけを貼り付けてください」と一言添える(引用符ごとコピーしてしまい、接続エラーになるケースを防ぐため)
